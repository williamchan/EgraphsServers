{ # Configures the live version of the site.
  #   The cloudbees target should be aliased by www.egraphs.com
  include "data/base"
  include "data/prod_base"
  include "data/live_services"
  include "data/ipad_latest"

  cloudbees.applicationId=oxygen
  application.id=live
  allowDemoPurchase=false
  admin.tools.enabled=restricted
  adminreview.skip=false
  cdn.contenturl=d3kp0rxeqzwisk.cloudfront.net

  # ToyBox config
  toybox.is-private=false
}
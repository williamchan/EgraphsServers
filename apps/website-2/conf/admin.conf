{ # Configures the admin-only version of the site.
  #  As of the time of writing, this application deploys onto
  #  boron.egraphs.com
  include "data/base"
  include "data/prod_base"
  include "data/live_services"
  include "data/ipad_latest"
  
  cloudbees.applicationId=boron
  application.id=boron

  admin.tools.enabled=full
  adminreview.skip=false
}
# Application dependencies

require:
    - egraphs-modules -> frontend HEAD
    - play        
    - play -> scala 0.9.1
    - play -> less 0.9.1

repositories:
    - Module Folder:
        type:       local
        artifact:   ${application.path}/../../modules/[module]
        contains:
            - egraphs-modules